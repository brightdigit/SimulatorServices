name: SimulatorServices
settings:
  LINT_MODE: ${LINT_MODE}
packages:
  SimulatorServices:
    path: .
aggregateTargets:
  Lint:
    buildScripts:
      - path: Scripts/lint.sh
        name: Lint
        basedOnDependencyAnalysis: false 
    schemes: {}